# Config Notes

`paths.json` contains starter defaults for a local checkout.

- Adjust `lora_root` to match your own LoRA library location.
- `config/workflows/` is the public workspace for onboarded workflow templates.
- `config/workflows/staging/` is the drop zone for raw workflow exports before onboarding.
